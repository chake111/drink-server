<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.drink.server.mapper.UserMapper">

    <select id="getByOpenid" resultType="com.drink.server.entity.User">
        SELECT id, openid, nickname, avatar, phone, create_time
        FROM user WHERE openid = #{openid}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (openid, nickname, avatar)
        VALUES (#{openid}, #{nickname}, #{avatar})
    </insert>

    <select id="getById" resultType="com.drink.server.entity.User">
        SELECT id, openid, nickname, avatar, phone, create_time
        FROM user WHERE id = #{id}
    </select>

</mapper>
